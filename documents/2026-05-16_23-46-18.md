# 2026-05-16 23:46:18

## Yazılan Prompt

> Project formunu DB şemasıyla eşitle

## Yapılan Plan

Mevcut form ile `projects` tablosu arasındaki alan uyuşmazlıkları tespit edildi.

**Fark analizi:**

| DB kolonu | Eski form durumu |
|---|---|
| `title` | ✅ `name` field'ından map ediliyordu |
| `title_tr` | ❌ Eksik |
| `description` | ✅ Vardı |
| `description_tr` | ❌ Eksik |
| `github_link` | ⚠️ `link` adıyla geliyordu (belirsiz) |
| `live_link` | ❌ Eksik |
| `image_url` | ❌ Eksik |
| `technologies` | ❌ Eksik |

**Strateji:**
1. `projectFormSchema` → tüm DB kolonlarını kapsayacak şekilde genişlet
2. `addProject` action → yeni alanları oku, `technologies` string'ini TEXT[] array'e dönüştür
3. Form sayfası → 2'li sütun layout (EN/TR yan yana), ayrı github/live link alanları

## Yapılan İşlem

### `src/lib/validations/project.ts`
- `link` → `github_link` + `live_link` (ayrıldı)
- `name_tr`, `description_tr`, `image_url`, `technologies` eklendi
- `urlOrEmpty` yardımcı tipi oluşturuldu

### `src/app/[locale]/admin/projects/actions.ts`
- Tüm yeni form alanları FormData'dan okunuyor
- `technologies` string → `split(',').map(trim).filter(Boolean)` → TEXT[]
- DB insert: `title_tr`, `description_tr`, `live_link`, `image_url`, `technologies` eklendi

### `src/app/[locale]/admin/projects/new/page.tsx`
- Tekrarlayan input bloklarını azaltmak için `Field` bileşeni eklendi
- EN/TR alanları 2 sütunlu grid layout ile yan yana gösteriliyor
- GitHub Link ve Live Link ayrı alanlar olarak eklendi
- Image URL ve Technologies (virgülle ayrılmış) alanları eklendi

### Komutlar

```bash
npm run lint   # ✅ Hata yok
git commit -m "feat(admin): align project form with DB schema"
```

### Commit

`256fd84` — `feat(admin): align project form with DB schema`
