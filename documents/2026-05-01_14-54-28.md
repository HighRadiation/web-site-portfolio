# 2026-05-01 14:54:28 - Navbar Social Media Relocation

## Yazılan Prompt
Relocate the social media icon group to the right side of the navigation bar, adjacent to the language action group. Implement a flexible spacing strategy using auto margins to separate the primary navigation links from the secondary action groups.

## Yapılan Plan
1. `src/app/globals.css` dosyasındaki `.nav-socials` sınıfına `margin-left: auto` eklenerek grubun sağa yaslanması.
2. Daha önce `.nav-actions` sınıfında bulunan `margin-left: auto` özelliğinin kaldırılması (artık `.nav-socials` bu görevi üstleniyor).
3. Navigasyon linkleri ile aksiyon grupları arasında geniş bir boşluk oluşturularak görsel dengenin sağlanması.
4. Değişikliklerin kaydedilmesi ve commit edilmesi.

## Yapılan İşlem
- `globals.css` içerisinde:
    - `.nav-actions` sınıfından `margin-left: auto` kaldırıldı.
    - `.nav-socials` sınıfına `margin-left: auto` eklendi. Bu sayede hem ikonlar hem de dil seçicisi bir bütün olarak sağa yaslanmış oldu.
- `git commit -m "style: relocate social media icons to the right"` komutu ile değişiklik commit edildi.
