# 2026-05-17 00:37:47

## Yazılan Prompt

> 5. Navbar + Footer sosyal medya linkleri → İki dosyada da aynı GitHub/LinkedIn/Instagram URL'leri tekrar ediyor. components/ui/SocialLinks.tsx çıkar, ikisinden de çağır.

## Yapılan Plan

Kod kalitesini artırmak, kod tekrarını (WET) önlemek ve DRY (Don't Repeat Yourself) prensiplerini uygulamak amacıyla Navbar ve Footer bileşenlerindeki tekrarlayan sosyal medya bağlantı adreslerinin ortak bir bileşende merkezileştirilmesi planlandı.

**Plan:**
1. Yeni, tekrar kullanılabilir ve özelleştirilebilir bir `SocialLinks` bileşeni `src/components/ui/SocialLinks.tsx` konumunda oluşturulacak.
2. Bileşen, hem Navbar'daki ikonlu (SVG) yerleşimi hem de Footer'daki düz metin (text) yerleşimini destekleyecek şekilde `variant?: 'icon' | 'text'` özelliğine (prop) sahip olacak.
3. `Navbar.tsx` ve `Footer.tsx` bileşenlerindeki tüm manuel sosyal medya linkleri silinecek ve yerlerine yeni `SocialLinks` bileşeni entegre edilecek.
4. Kod derleme ve tip kontrolleri yapılarak commitlenecek.

## Yapılan İşlem

### Yeni Bileşen Oluşturuldu

**`src/components/ui/SocialLinks.tsx` [NEW]**
- GitHub, LinkedIn ve Instagram bağlantı adresleri, etiketleri ve SVG ikonları tek bir `SOCIAL_LINKS` dizisinde (array) tanımlandı.
- `SocialLinks` fonksiyonel bileşeni oluşturularak `variant` değerine göre SVG ikonlu veya düz metin bağlantıları döndürmesi sağlandı. TypeScript tip güvenliği ve ESLint standartlarına tam uyum gösterildi.

### Güncellenen Bileşenler

**`src/components/layout/Navbar.tsx`**
- Eski el ile eklenmiş SVG sosyal medya kod blokları silindi.
- Yerine yeni `SocialLinks` bileşeni dahil edildi:
  ```tsx
  <SocialLinks variant="icon" className="nav-socials" />
  ```

**`src/components/layout/Footer.tsx`**
- El ile eklenmiş düz metin sosyal medya linkleri kaldırıldı.
- Yerine `SocialLinks` bileşeni metin moduyla entegre edildi:
  ```tsx
  <SocialLinks variant="text" className="footer-links" />
  ```

### Komutlar
```bash
npm run lint && npm run build # (Kontroller 100% başarılı)
git add src/components/ui/SocialLinks.tsx src/components/layout/Navbar.tsx src/components/layout/Footer.tsx
git commit -m "refactor(socials): extract reusable SocialLinks component for Navbar and Footer"
```

### Commit
`31af10d` — `refactor(socials): extract reusable SocialLinks component for Navbar and Footer`
