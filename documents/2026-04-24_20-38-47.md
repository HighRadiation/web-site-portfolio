# İşlem Kaydı: 2026-04-24 20:38:47

## 1. Yazılan Prompt
@[c:\Users\bugra\OneDrive\Desktop\myWebsite\references\portfolio_backend_roadmap.svg] 

Adım 1 — Proje Kurulumu
create-next-app ile TypeScript init. @supabase/supabase-js, @supabase/ssr kur. .env.local'e SUPABASE_URL ve SUPABASE_ANON_KEY ekle. /lib/supabase/server.ts ve client.ts helper oluştur.

## 2. Yapılan Plan
1. `create-next-app` kullanılarak TypeScript ve App Router tabanlı bir Next.js projesi başlatılması.
2. `@supabase/supabase-js` ve `@supabase/ssr` paketlerinin yüklenmesi.
3. `.env.local` dosyasına gerekli Supabase değişkenlerinin eklenmesi.
4. `src/lib/supabase` altında client ve server helper dosyalarının oluşturulması.

## 3. Yapılan İşlem
- `npx create-next-app@latest temp-app --typescript --no-tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --no-git --agents-md` komutu çalıştırıldı.
- `temp-app` klasöründeki tüm dosyalar root dizinine taşındı ve geçici klasör silindi.
- `package.json` içerisindeki proje ismi `my-website` olarak güncellendi.
- `npm install @supabase/supabase-js @supabase/ssr` ile paketler yüklendi.
- `.env.local` oluşturuldu (yer tutucu değerlerle).
- `src/lib/supabase/client.ts` ve `src/lib/supabase/server.ts` dosyaları Next.js 15+ (async cookies) uyumlu şekilde oluşturuldu.
