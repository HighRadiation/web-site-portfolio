# 2026-05-16 23:43:10

## Yazılan Prompt

> Ölü admin API route'larını sil

## Yapılan Plan

`src/app/api/admin/` altındaki tüm route'ların gerçekten kullanılıp kullanılmadığı araştırıldı.

**Analiz:**
- Tüm admin CRUD operasyonları `src/app/[locale]/admin/**/actions.ts` içindeki **Server Actions** tarafından gerçekleştiriliyor
- `fetch('/api/admin/...')` şeklinde hiçbir çağrı bulunamadı
- 3 route dosyası tamamen ölü kod — gereksiz saldırı yüzeyi oluşturuyordu
- `openapi.json` bu endpoint'leri hâlâ belgeliyordu, o referanslar da temizlendi

**Silinen route'lar:**

| Dosya | Method | Endpoint |
|---|---|---|
| `api/admin/profile/route.ts` | PUT | `/api/admin/profile` |
| `api/admin/projects/route.ts` | POST | `/api/admin/projects` |
| `api/admin/projects/[id]/route.ts` | DELETE | `/api/admin/projects/{id}` |

## Yapılan İşlem

### Silinen Dosyalar

```
src/app/api/admin/profile/route.ts
src/app/api/admin/projects/route.ts
src/app/api/admin/projects/[id]/route.ts
```

```bash
rm -rf src/app/api/admin/
```

### Güncellenen Dosya

**`src/lib/swagger/openapi.json`** — Ölü endpoint path'leri ve ilgili schema'lar temizlendi:
- Kaldırılan path'ler: `/api/admin/profile`, `/api/admin/projects`, `/api/admin/projects/{id}`
- Kaldırılan schema'lar: `Profile`, `ProfileUpdate`, `Project`, `ProjectCreate`
- Kaldırılan securityScheme: `cookieAuth`

### Komutlar

```bash
npm run lint   # ✅ Hata yok
git add -A
git commit -m "chore(api): remove dead admin API routes"
```

### Commit

`41aa38f` — `chore(api): remove dead admin API routes`
