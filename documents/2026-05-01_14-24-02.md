# 2026-05-01 14:24:02 - Navbar Link Alignment Update

## Yazılan Prompt
Adjust the navigation link alignment within the navbar-inner container from a centered layout to a left-aligned layout. Remove legacy offset padding and ensure consistent spacing between the links and the container boundaries.

## Yapılan Plan
1. `src/app/globals.css` dosyasındaki `.nav-links` sınıfının `justify-content` değerinin `center`'dan `flex-start`'a çekilmesi.
2. Linkleri ortalamak için kullanılan ve dengesizlik yaratan `padding-left: 4rem` değerinin kaldırılması.
3. Değişikliklerin kaydedilmesi ve commit edilmesi.

## Yapılan İşlem
- `src/app/globals.css` içerisinde `.nav-links` stili şu şekilde güncellendi:
    - `justify-content: flex-start` yapılarak linkler sola yaslandı.
    - `padding-left: 4rem` satırı silinerek asimetrik boşluk kaldırıldı.
- `git commit -m "style: align navbar links to the left"` komutu ile değişiklik commit edildi.
