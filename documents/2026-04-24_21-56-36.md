# İşlem Kaydı: 2026-04-24 21:56:36

## 1. Yazılan Prompt
Adım 4 — Core Modüller (API Routes)
/app/api/profile/route.ts, /app/api/projects/route.ts, /app/api/skills/route.ts oluştur. Her route'ta supabase.from('table').select(). TypeScript interface'leri /types/ altında tanımla.

## 2. Yapılan Plan
1. `src/types` dizini altında tablo yapıları için TypeScript arayüzlerinin (`interface`) tanımlanması:
    - `profile.ts`
    - `project.ts`
    - `skill.ts`
2. API rotalarının oluşturulması:
    - `src/app/api/profile/route.ts`: Profil bilgilerini getiren GET isteği.
    - `src/app/api/projects/route.ts`: Projeleri getiren GET isteği.
    - `src/app/api/skills/route.ts`: Yetenekleri getiren GET isteği.
3. Her rotada `src/lib/supabase/server.ts` üzerinden Supabase istemcisinin kullanılması.

## 3. Yapılan İşlem
- `src/types/database.ts` dosyası oluşturuldu; `Profile`, `Project` ve `Skill` interface'leri tanımlandı.
- `src/app/api/profile/route.ts` oluşturuldu (Tekil veri çekmek üzere yapılandırıldı).
- `src/app/api/projects/route.ts` oluşturuldu (Tarihe göre sıralı liste).
- `src/app/api/skills/route.ts` oluşturuldu (Yetkinliğe göre sıralı liste).
- Tüm rotalarda `@supabase/ssr` server istemcisi kullanıldı.
