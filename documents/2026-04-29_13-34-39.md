# Dokümantasyon: 2026-04-29_13-34-39

## Yazılan Prompt
5 sorun tespit edildi (contact_messages read kolonu, hardcoded UUID, skill
kategorileri çevirisi, service role key fallback, çift ESLint config)

## Yapılan Plan
1. `contact_messages` tablosuna `read` kolonu için yeni migration oluştur.
2. `20260429000000_fix_timeline_permissions.sql` içindeki hardcoded UUID
   kaldırılarak dinamik hale getirilecek.
3. `LanguageContext.tsx` içine `systems`, `design`, `mobile_web`, `ai`
   anahtarları eklenecek.
4. `src/app/api/contact/route.ts` dosyasında service role key fallback
   kaldırılacak; eksikse hata fırlatılacak.
5. `.eslintrc.json` dosyası mevcut değil; ESLint config zaten tekil.

## Yapılan İşlem
- [x] Migration: `contact_messages` tablosuna `read` kolonu eklenmesi.
- [x] Migration: Hardcoded UUID yerine dinamik atama.
- [x] `LanguageContext.tsx`: Eksik skill kategorileri çevirileri.
- [x] `route.ts`: Service role key güvenlik açığı.
- [x] ESLint config tekil (sorun yok).
