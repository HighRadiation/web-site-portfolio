# İşlem Kaydı: 2026-04-24 22:32:29

## 1. Yazılan Prompt
Hatalar vardı düzelttim, düzelttiğim dosyalara bak önce ondan sonra bu adım.
Adım 6 — Admin CRUD
PUT /api/admin/profile, POST /api/admin/projects, DELETE /api/admin/projects/[id]. Her route başında getUser() ile session doğrula. RLS policy'leri: INSERT/UPDATE/DELETE sadece authenticated user.

## 2. Yapılan Plan
1. Kullanıcının `projects/route.ts` ve `skills/route.ts` üzerinde yaptığı düzeltmelerin incelenmesi (Server helper yerine doğrudan `@supabase/supabase-js` kullanımı).
2. Admin işlemleri için yeni API rotalarının oluşturulması:
    - `src/app/api/admin/profile/route.ts` (PUT)
    - `src/app/api/admin/projects/route.ts` (POST)
    - `src/app/api/admin/projects/[id]/route.ts` (DELETE)
3. Her rotada yetki kontrolü (`auth.getUser()`) yapılması.
4. Admin işlemlerinde oturum bilgisi gerektiği için `src/lib/supabase/server.ts` helper'ının kullanılması (Eğer hata verirse alternatif çözümler üretilmesi).
5. Yeni kurallara uygun (English, Conventional) commit yapılması.

## 3. Yapılan İşlem
- Kullanıcının `projects/route.ts` ve `skills/route.ts` üzerindeki "manuel" Supabase istemcisi düzeltmesi incelendi (SSR helper'ın pasifize edildiği görüldü).
- `src/app/api/admin/profile/route.ts` (PUT) oluşturuldu. Auth kontrolü eklendi.
- `src/app/api/admin/projects/route.ts` (POST) oluşturuldu. Auth kontrolü eklendi.
- `src/app/api/admin/projects/[id]/route.ts` (DELETE) oluşturuldu. Auth ve sahiplik kontrolü eklendi.
- Tüm Admin rotalarında `src/lib/supabase/server.ts` üzerinden oturum doğrulama (`getUser()`) zorunlu kılındı.
- Yapılan değişiklikler İngilizce commit mesajı ile kaydedildi.
