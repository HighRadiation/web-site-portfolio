# 2026-05-02 01:43:00 - Navigasyon Hizalama Hassas Ayarı

## Yazılan Prompt
Navigasyon menüsü üzerinden yapılan bölüm geçişlerinde, içeriğin ekranın üst kısmına olan mesafesini artırmak (daha aşağıda konumlandırmak) amacıyla `scroll-margin-top` değeri `7rem` olarak güncellenmiştir. Bu sayede bölümler navbarın hemen altında daha ferah bir boşlukla başlamaktadır.

## Yapılan Plan
Kullanıcı geri bildirimi üzerine sayfa içi yönlendirme (anchor scroll) hizalaması revize edildi:

1.  **CSS Hizalama (`globals.css`):**
    *   `.section` ve `.section-alt` sınıfları için `scroll-margin-top` değeri `5rem`'den `7rem`'e çıkarıldı.
    *   Bu değişiklik, "Projects" veya "About" gibi linklere tıklandığında sayfanın biraz daha aşağı kaymasını sağlayarak içeriğin üstten daha fazla nefes almasına olanak tanıyor.

## Yapılan İşlem
- `src/app/globals.css` dosyasında `scroll-margin-top` değerleri güncellendi.
