# Dokümantasyon: 2026-04-29_01-38-12

## Yazılan Prompt
3 açık var

**1. `eslint.config.mjs.bak` repoda** — silmelisin.
```bash
git rm eslint.config.mjs.bak
```

**2. `eslint-config-next` eksik `.eslintrc.json`'da** — Next.js projesinde olmalı:
```json
"extends": [
  "next/core-web-vitals",
  "eslint:recommended",
  "plugin:@typescript-eslint/recommended"
]
```

**3. `README.md` yanlış repo URL** — şunu gösteriyor:
```
git clone https://github.com/HighRadiation/web-site-portfolio.git
```
Doğrusu `bugraoksuz.me.git`.

## Yapılan Plan
1. `eslint.config.mjs.bak` dosyasını `git rm` komutu ile silecek.
2. `.eslintrc.json` dosyasını `next/core-web-vitals` kuralını içerecek şekilde güncelleyecek.
3. `README.md` dosyasındaki yanlış repo URL'ini düzeltecek.
4. Her bir adım için ilgili commit mesajları ile işlem yapacak.

## Yapılan İşlem
- `eslint.config.mjs.bak` dosyası `git rm eslint.config.mjs.bak` komutu ile silindi ve `chore: remove unused eslint backup file` mesajı ile commit edildi.
- `.eslintrc.json` dosyasına `next/core-web-vitals` kuralı eklendi ve `fix: add next/core-web-vitals to eslint config` mesajı ile commit edildi.
- `README.md` dosyasındaki repo URL'i `https://github.com/HighRadiation/bugraoksuz.me.git` olarak güncellendi ve `docs: update repository URL in README` mesajı ile commit edildi.
