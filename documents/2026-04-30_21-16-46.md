## Yazılan Prompt
Fix the following issues in this Next.js 16 project. Apply all changes, run `npm run lint` and `npm run format` after each file change, then commit each fix separately with conventional commit messages in English.

---

### Fix 1 — src/app/api/contact/route.ts
Remove detailed DB error logging. Replace:
```ts
console.error('Database Error Details:', {
  message: dbError.message,
  details: dbError.details,
  hint: dbError.hint,
  code: dbError.code,
});
```
With:
```ts
console.error('DB insert failed');
```
Commit: `fix: remove detailed db error logging from contact route`

---

### Fix 2 — src/components/sections/Projects.tsx
Change Turkish console.error to English:
```ts
// Before
console.error('Frontend proje çekme hatası:', error.message);

// After
console.error('Failed to fetch projects:', error.message);
```
Commit: `fix: translate turkish console.error to english in ProjectsSection`

---

### Fix 3 — src/proxy.ts
Remove or translate Turkish/broken comments:
```ts
// Remove this line entirely:
// Where is this come from?

// Replace this:
// Bu işlem oturumu yenileyecektir (refresh token)
// With:
// Refresh the user session via Supabase SSR client
```
Commit: `fix: clean up comments in proxy middleware`

---

### Fix 4 — src/app/admin/projects/page.tsx
Replace empty error block with a console.error:
```ts
// Before
if (error) {
  // Silently handle error or show a generic error state without leaking schema details
}

// After
if (error) {
  console.error('Failed to fetch projects from database');
}
```
Commit: `fix: handle supabase error in admin projects page`

---

### After all fixes
Run:
```bash
npm run lint
npm run format
git push origin main
```

Then create a documentation file at `documents/YYYY-MM-DD_HH-mm-ss.md` (use current timestamp) with this structure:
- **Yazılan Prompt**: (this prompt)
- **Yapılan Plan**: Fix 4 security/code quality issues found in code review
- **Yapılan İşlem**: List each changed file and what was changed

## Yapılan Plan
Fix 4 security/code quality issues found in code review

## Yapılan İşlem
- **`src/app/api/contact/route.ts`**: Removed detailed database error logging to prevent schema leakage, replacing it with a generic `console.error('DB insert failed')`.
- **`src/components/sections/Projects.tsx`**: Translated the Turkish error message `'Frontend proje çekme hatası:'` to English `'Failed to fetch projects:'`.
- **`src/proxy.ts`**: Removed the obsolete `Where is this come from?` comment and translated other Turkish comments (e.g., `Bu işlem oturumu yenileyecektir`) into clear English explanations (`Refresh the user session via Supabase SSR client`).
- **`src/app/admin/projects/page.tsx`**: Added an error log `console.error('Failed to fetch projects from database')` to the empty error handler to improve debugging without exposing schema details.
- **`src/components/sections/ProjectsSectionClient.tsx`**: Added an explicit return type (`string`) to an IIFE block to satisfy the `explicit-function-return-type` ESLint rule during the format/lint phase.
- **Terminal İşlemleri**: Tüm dosyalar için `npm run format` ve `npm run lint` çalıştırıldı. Sonrasında her dosyadaki değişiklik ilgili Conventional Commit mesajıyla ayrı ayrı commitlendi ve github'a `git push origin main` ile gönderildi.
