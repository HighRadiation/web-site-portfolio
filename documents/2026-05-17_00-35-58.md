# 2026-05-17 00:35:58

## Yazılan Prompt

> 4. Rate limit 5/dk → 10/dk → api/contact/route.ts:11 getRateLimiter(5, '1 m'). NAT/ofis ağları için 5 sıkı.

## Yapılan Plan

İletişim formu API rotasındaki (`src/app/api/contact/route.ts`) istek sınırlandırma (Rate Limit) ayarının, aynı dış IP'yi paylaşan NAT veya kurumsal/ofis ağlarındaki gerçek kullanıcıların yanlışlıkla engellenmesini (false positives) önlemek amacıyla genişletilmesi planlandı.

**Plan:**
1. Dakika başına izin verilen maksimum istek limiti 5'ten 10'a çıkarılacak (`getRateLimiter(10, '1 m')`).
2. Bu sayede hem spam saldırılarına karşı güçlü koruma korunmuş olacak, hem de ortak ağlardaki kullanıcı deneyimi iyileştirilecek.
3. Değişiklik derlenip test edilecek ve commitlenecek.

## Yapılan İşlem

### Değiştirilen Dosyalar

**`src/app/api/contact/route.ts`**
- İstek sınırlandırma tanımı güncellendi:
  ```typescript
  const limiter = getRateLimiter(10, '1 m');
  ```

### Komutlar
```bash
npm run lint && npm run build # (Kontroller 100% başarılı)
git add src/app/api/contact/route.ts
git commit -m "fix(contact): relax redis rate limiter to 10 requests per minute"
```

### Commit
`c208035` — `fix(contact): relax redis rate limiter to 10 requests per minute`
