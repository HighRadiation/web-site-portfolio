# 2026-05-02 01:56:00 - Terminal Arka Plan Yumuşatması

## Yazılan Prompt
Terminal penceresinin zifiri siyah (`#0c0c0c`) olan arka planı, daha modern bir görünüm için yarı şeffaf koyu gri (`rgba(22, 22, 22, 0.8)`) ile değiştirilmiştir. Pencereye `backdrop-filter: blur(12px)` ve daha ince bir sınır çizgisi (`border`) eklenerek, sitenin genel 'glassmorphism' estetiği modal penceresine de yansıtılmıştır.

## Yapılan Plan
Terminal penceresinin çok koyu ve sert görünmesini engellemek için şu stil güncellemeleri yapıldı:

1.  **Renk ve Saydamlık (`globals.css`):**
    *   `background-color` değeri opak siyahtan şeffaf koyu griye çekildi.
    *   `backdrop-filter: blur(12px)` eklenerek pencerenin arkasındaki içeriği hafifçe yansıtması sağlandı.
    *   `border` rengi daha ince ve zarif bir beyaz opaklığa (`rgba(255, 255, 255, 0.1)`) dönüştürüldü.
    *   `box-shadow` derinliği yumuşatılarak pencerenin ekranda daha hafif durması sağlandı.

## Yapılan İşlem
- `src/app/globals.css` dosyasında terminal penceresi stilleri modernize edildi.
