# İşlem Kaydı: 2026-04-24 22:37:48

## 1. Yazılan Prompt
Adım 7 — Contact + Güvenlik
POST /api/contact → contact_messages tablosuna insert. Rate limiting: Vercel'de @upstash/ratelimit + Redis kullan. Input validation: zod schema ile her route'ta parse et.

## 2. Yapılan Plan
1. Supabase üzerinde `contact_messages` tablosu oluşturulması ve sadece kayıtlı kullanıcıların (admin) görebilmesi için RLS (Row Level Security) ayarlanması.
2. Form verilerinin backend'de güvenle doğrulanabilmesi için `zod`, hız sınırlaması için ise `@upstash/ratelimit` ve `@upstash/redis` paketlerinin yüklenmesi.
3. `/api/contact` API endpoint'inin (ROUTE) oluşturulması:
    - İstek (Request) gövdesinin Zod ile doğrulanması.
    - Redis (Upstash) ile IP tabanlı dakikada 5 istek sınırı getirilmesi.
    - Doğrulanan verilerin `supabase.from('contact_messages')` kullanılarak veritabanına eklenmesi.
4. Conventional Commits standartlarında İngilizce mesajla commit atılması.

## 3. Yapılan İşlem
- `supabase/migrations/20260424000001_contact_messages.sql` dosyası oluşturuldu ve `supabase db push` ile veritabanına uygulandı.
- `npm install @upstash/ratelimit @upstash/redis zod` ile paketler dahil edildi.
- `src/lib/validations/contact.ts` Zod şeması kodlandı.
- `.env.local`'e Redis değişkeni yer tutucuları eklendi.
- `src/app/api/contact/route.ts` oluşturularak tüm güvenlik adımları tamamlandı.
